# Local dev notes with captured account credentials / session tokens — NEVER publish
solution.txt

# Obfuscation toolchain + CLEAN SOURCE — kept local only. The repo ships ONLY the obfuscated
# root JS (background/content/popup/sitelink.js). Edit src/*.js, then `npm run build` to regenerate.
src/
build.mjs
package.json
package-lock.json

# Integrity manifest — a build artifact that belongs on the proxy (served via /config), not in the
# shipped extension. Regenerated by `npm run build`; copy into roo-proxy-server/ before deploying.
integrity.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
node_modules/
